---
phase: 01-authentication-graphql-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/main/kotlin/nesski/de/services/SystmAuthService.kt
  - src/main/kotlin/nesski/de/plugins/SystmAuthentication.kt
  - config/systm.yaml
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
  - DATA-02

must_haves:
  truths:
    - User configures Systm credentials in config file (YAML)
    - Application reads credentials and authenticates via GraphQL login mutation
    - User's Systm token is persisted in encrypted session
    - User can clear/reset their Systm token
    - Application sends Bearer JWT token with GraphQL requests
  artifacts:
    - path: config/systm.yaml
      provides: User credentials configuration
      contains: systm.username, systm.password
    - path: src/main/kotlin/nesski/de/services/SystmAuthService.kt
      provides: Login mutation from config credentials
      contains: login() function
    - path: src/main/kotlin/nesski/de/plugins/SystmAuthentication.kt
      provides: Auth routes + logout + auto-login on startup
      contains: GET /systm-logout, auto-auth from config
---
  key_links:
    - from: SystmLoginRoute.kt
      to: SystmAuthService.kt
      via: service call
      pattern: systmAuthService.login
    - from: SystmLoginRoute.kt
      to: SystmSession.kt
      via: call.sessions.set()
      pattern: sessions.set.*SystmSession
    - from: SystmGraphQLClient.kt
      to: SystmSession.kt
      via: token from session
      pattern: Authorization.*Bearer
---

<objective>
Implement user config file (YAML) for Systm credentials and auto-login functionality.
</objective>

<tasks>

<task type="auto">
  <name>Create Systm config file (YAML)</name>
  <files>config/systm.yaml</files>
  <action>
Create config/systm.yaml with:
```yaml
systm:
  username: "your-email@example.com"
  password: "your-password"
```

Follow existing config patterns (check for existing YAML/TOML configs in the project)
  </action>
  <verify>Config file created with valid YAML structure</verify>
  <done>config/systm.yaml with username/password fields</done>
</task>

<task type="auto">
  <name>Update SystmAuthService to read from config</name>
  <files>src/main/kotlin/nesski/de/services/SystmAuthService.kt</files>
  <action>
Update SystmAuthService:
1. Load credentials from config/systm.yaml on startup
2. login() function uses config credentials (no parameters needed)
3. Auto-authenticate on application startup if credentials available
4. Request/Response data classes for login mutation

Note: No local JWT validation - Systm validates on each API call
  </action>
  <verify>Service compiles, reads config, auto-authenticates on startup</verify>
  <done>SystmAuthService reads credentials from config</done>
</task>

<task type="auto">
  <name>Create SystmAuthentication plugin with routes</name>
  <files>src/main/kotlin/nesski/de/plugins/SystmAuthentication.kt</files>
  <action>
Create SystmAuthentication plugin:
1. Auto-login on startup using config credentials
2. GET /systm-logout - clears session and redirects to error (config-based, no re-login)
3. GET /systm-status - returns auth status (logged in, not logged in)

Follow existing plugin patterns from WahooAuthenticationOauth2.kt
  </action>
  <verify>Plugin registers, auto-login works, logout clears session</verify>
  <done>SystmAuthentication plugin with routes</done>
</task>

</tasks>

<verification>
- [ ] config/systm.yaml created with username/password
- [ ] Application reads config on startup
- [ ] Auto-login occurs with config credentials
- [ ] Token stored in encrypted session
- [ ] /systm-logout clears session
</verification>

<success_criteria>
User configures Systm credentials in YAML file, application auto-authenticates on startup, token persisted in encrypted session</success_criteria>

<output>
After completion, create .planning/phases/01-authentication-graphql-setup/01-02-SUMMARY.md
</output>
