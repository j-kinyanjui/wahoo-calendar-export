---
phase: 01-authentication-graphql-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/nesski/de/plugins/SystmAuthentication.kt
  - src/main/kotlin/nesski/de/models/SystmSession.kt
  - src/main/kotlin/nesski/de/plugins/SystmGraphQLClient.kt
autonomous: true
requirements:
  - AUTH-02
  - DATA-01

must_haves:
  truths:
    - User's Systm token stored in encrypted session
    - Application can make GraphQL requests to api.thesufferfest.com
  artifacts:
    - path: src/main/kotlin/nesski/de/models/SystmSession.kt
      provides: Session data class for Systm token storage
      contains: data class SystmSession
    - path: src/main/kotlin/nesski/de/plugins/SystmAuthentication.kt
      provides: Session configuration for Systm
      contains: install(Sessions) with cookie<SystmSession>
    - path: src/main/kotlin/nesski/de/plugins/SystmGraphQLClient.kt
      provides: GraphQL client for Systm API
      contains: HttpClient extension for POST /graphql
  key_links:
    - from: SystmAuthentication.kt
      to: SystmSession.kt
      via: cookie<UserSession> pattern
      pattern: cookie<SystmSession>
---

<objective>
Set up session management and GraphQL client infrastructure for Systm authentication.
</objective>

<tasks>

<task type="auto">
  <name>Create SystmSession model</name>
  <files>src/main/kotlin/nesski/de/models/SystmSession.kt</files>
  <action>
Create data class SystmSession similar to existing UserSession. Include:
- token: String (JWT token from Systm)
- expiresAt: Long (expiration timestamp in millis)
- isExpired(): Boolean method
- companion object with getSession(call: ApplicationCall): SystmSession?

Follow existing UserSession pattern from src/main/kotlin/nesski/de/models/UserSession.kt
  </action>
  <verify>File exists with valid Kotlin syntax, compiles with project</verify>
  <done>SystmSession data class with token, expiresAt, isExpired() method, and getSession() companion method</done>
</task>

<task type="auto">
  <name>Configure Systm session plugin</name>
  <files>src/main/kotlin/nesski/de/plugins/SystmAuthentication.kt</files>
  <action>
Create SystmAuthentication plugin following WahooAuthenticationOauth2 pattern:
- Install Sessions config with cookie<SystmSession>("systm_session")
- Add session configuration to Application.module()

Reference: src/main/kotlin/nesski/de/plugins/WahooAuthenticationOauth2.kt lines 33-36 for session pattern
  </action>
  <verify>Session plugin installs without errors</verify>
  <done>Sessions plugin installed with SystmSession cookie</done>
</task>

<task type="auto">
  <name>Create GraphQL client for Systm</name>
  <files>src/main/kotlin/nesski/de/plugins/SystmGraphQLClient.kt</files>
  <action>
Create GraphQL client wrapper for Systm API:
- Define GraphQL request/response data classes (GraphQLRequest, GraphQLResponse, GraphQLError)
- Create executeQuery() suspend function that:
  - POSTs to https://api.thesufferfest.com/graphql
  - Sets Content-Type: application/json
  - Accepts query string and variables map
  - Returns GraphQLResponse<T>
- Handle HTTP errors (non-200 responses)

Reference: RESEARCH.md patterns for GraphQL client
  </action>
  <verify>File exists, compiles, HttpClient can POST to GraphQL endpoint</verify>
  <done>GraphQL client can execute POST requests to api.thesufferfest.com/graphql</done>
</task>

</tasks>

<verification>
- [ ] SystmSession.kt compiles with project
- [ ] SystmAuthentication.kt installs without errors  
- [ ] SystmGraphQLClient.kt has working executeQuery function
</verification>

<success_criteria>
Session infrastructure in place for storing Systm token, GraphQL client ready for queries</success_criteria>

<output>
After completion, create .planning/phases/01-authentication-graphql-setup/01-01-SUMMARY.md
</output>
