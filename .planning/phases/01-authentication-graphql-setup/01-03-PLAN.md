---
phase: 01-authentication-graphql-setup
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/main/kotlin/nesski/de/routes/SystmPlansRoute.kt
  - src/main/kotlin/nesski/de/services/SystmPlansService.kt
  - src/main/kotlin/nesski/de/models/SystmModels.kt
autonomous: true
requirements:
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - Application executes GetUserPlansRange GraphQL query with date parameters
    - Application handles GraphQL error responses (HTTP 200 with errors field) gracefully
  artifacts:
    - path: src/main/kotlin/nesski/de/services/SystmPlansService.kt
      provides: Fetch plans with GetUserPlansRange query
      contains: fetchPlans() function
    - path: src/main/kotlin/nesski/de/routes/SystmPlansRoute.kt
      provides: Plans route using service
      contains: GET /systm-plans
    - path: src/main/kotlin/nesski/de/models/SystmModels.kt
      provides: Data models for Systm responses
      contains: Plan, Workout types
  key_links:
    - from: SystmPlansRoute.kt
      to: SystmPlansService.kt
      via: service call
      pattern: systmPlansService.fetchPlans
    - from: SystmPlansService.kt
      to: SystmGraphQLClient.kt
      via: GraphQL query execution
      pattern: executeQuery.*GetUserPlansRange
    - from: SystmPlansService.kt
      to: SystmSession.kt
      via: token from session
      pattern: Authorization.*Bearer
---

<objective>
Implement GetUserPlansRange query execution with error handling.
</objective>

<tasks>

<task type="auto">
  <name>Create Systm data models</name>
  <files>src/main/kotlin/nesski/de/models/SystmModels.kt</files>
  <action>
Create data models for Systm GraphQL responses:
- GetUserPlansRangeResponse (wraps data and errors)
- UserPlansData (contains userPlansRange field)
- Plan (id, name, scheduledDate, status, type)
- GraphQLError (message, locations, path)
- GraphQLResponse<T> generic with data and errors nullable fields

Per RESEARCH.md - error field exists even on HTTP 200 responses
  </action>
  <verify>Models compile with kotlinx.serialization</verify>
  <done>Data models for GraphQL responses defined</done>
</task>

<task type="auto">
  <name>Create SystmPlansService with query and error handling</name>
  <files>src/main/kotlin/nesski/de/services/SystmPlansService.kt</files>
  <action>
Create SystmPlansService:
1. fetchPlans(token, startDate, endDate) function:
   - Query: GetUserPlansRange(startDate, endDate) { id, name, scheduledDate, status, type }
   - Default date range: LocalDate.now().minusDays(7) to LocalDate.now().plusDays(14)
   - Uses SystmGraphQLClient to execute query with Bearer token
   - Include Authorization: Bearer {token} header
   
2. Error handling:
   - Check response.errors != null before using response.data
   - Throw GraphQLException with error messages if errors present
   - Handle network errors with appropriate exceptions
   
Per CONTEXT.md: parse GraphQL error responses even when HTTP 200
  </action>
  <verify>fetchPlans() executes query, throws on GraphQL errors</verify>
  <done>SystmPlansService can fetch plans with proper error handling</done>
</task>

<task type="auto">
  <name>Create SystmPlansRoute</name>
  <files>src/main/kotlin/nesski/de/routes/SystmPlansRoute.kt</files>
  <action>
Create SystmPlansRoute:
1. GET /systm-plans - protected route requiring session:
   - Get SystmSession from call.sessions.get()
   - Check session not null and not expired
   - Call systmPlansService.fetchPlans() with default date range
   - Return JSON response with plans list
   - Return 401 if no valid session
   
2. Error handling:
   - Catch GraphQLException, return error message
   - Catch session expired, redirect to /systm-login

Follow pattern from WahooAuthenticationOauth2.kt lines 90-102
  </action>
  <verify>Route registers, returns plans with valid session, returns 401 without</verify>
  <done>Plans route working with auth check</done>
</task>

</tasks>

<verification>
- [ ] /systm-plans returns plans with valid token
- [ ] GraphQL errors cause proper exception handling
- [ ] Expired session redirects to login
- [ ] Default date range applied (past 7 + next 14 days)
</verification>

<success_criteria>
GetUserPlansRange query executes with Bearer token, GraphQL errors handled gracefully</success_criteria>

<output>
After completion, create .planning/phases/01-authentication-graphql-setup/01-03-SUMMARY.md
</output>
