---
phase: 06-docker-release-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified: []
autonomous: false
requirements:
  - CD-01
  - CD-02
  - CD-03

must_haves:
  truths:
    - "Pushing a commit to main triggers CI, then CD runs and pushes `latest` image to GHCR"
    - "Pushing a `v*.*.*` tag triggers CI, then CD runs and pushes version-tagged image to GHCR"
    - "Pushing a `v*.*.*` tag creates a GitHub Release visible at the repo's Releases page"
    - "Docker image is publicly pullable from GHCR without authentication"
  artifacts:
    - path: "https://github.com/j-kinyanjui/wahoo-plan-to-calendar/pkgs/container/wahoo-plan-to-calendar"
      provides: "Published GHCR package (latest + version tags)"
    - path: "https://github.com/j-kinyanjui/wahoo-plan-to-calendar/releases"
      provides: "GitHub Release for the pushed version tag"
  key_links:
    - from: "git push --tags"
      to: "GHCR image + GitHub Release"
      via: "cd.yml workflow triggered by CI completion"
      pattern: "workflow_run success"
---

<objective>
Verify the complete CD pipeline end-to-end: push a `v1.1.0` tag and confirm all three requirements are satisfied — GHCR `latest` (from prior main merge), GHCR versioned image, and GitHub Release with changelog.

Purpose: Human confirmation that the automation works correctly in the real GitHub environment. Automated YAML validation in plan 06-01 cannot verify actual workflow execution or GitHub API interactions.

Output: Confirmed working CD pipeline. GHCR package visibility set to public.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-docker-release-pipeline/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>task 1: commit and push cd.yml to trigger main-merge Docker build</name>
  <files></files>
  <action>
Commit and push the new cd.yml workflow to main. This will:
1. Trigger the CI workflow (ci.yml)
2. After CI succeeds, trigger the CD workflow (cd.yml) via workflow_run
3. CD will build and push the Docker image with `latest` tag to GHCR

Run:
```bash
git add .github/workflows/cd.yml
git commit -m "feat(06): add CD workflow for Docker build/push and GitHub releases"
git push origin main
```

Then monitor the Actions tab to confirm both CI and CD workflows complete successfully:
- Go to: https://github.com/j-kinyanjui/wahoo-plan-to-calendar/actions
- Wait for "CI" workflow to complete (green)
- Wait for "CD" workflow to complete (green)
- Confirm: Docker job shows "Build & Push Docker Image" step completed

This satisfies **CD-01**: Docker image pushed with `latest` tag on main merge.
  </action>
  <verify>
    <automated>
      # Confirm commit pushed successfully
      git log --oneline -1
      # Confirm cd.yml is on remote
      git ls-remote --heads origin main
    </automated>
  </verify>
  <done>
    - cd.yml committed and pushed to main
    - CI workflow triggered (visible in Actions tab)
    - CD workflow triggered after CI success
    - `latest` Docker image pushed to GHCR (visible at https://github.com/j-kinyanjui/wahoo-plan-to-calendar/pkgs/container/wahoo-plan-to-calendar)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>checkpoint 1: verify `latest` image published to GHCR and set package public</name>
  <action>Human verifies the CD workflow succeeded for main merge and sets GHCR package visibility to Public.</action>
  <what-built>
    cd.yml was pushed to main. CI ran, then CD ran. The Docker image should now be published to GHCR with the `latest` tag.
  </what-built>
  <how-to-verify>
    **Step 1 — Check workflow runs:**
    Visit: https://github.com/j-kinyanjui/wahoo-plan-to-calendar/actions
    - Confirm "CI" workflow run completed ✓
    - Confirm "CD" workflow run completed ✓ (may take a few minutes after CI)

    **Step 2 — Verify GHCR package:**
    Visit: https://github.com/j-kinyanjui/wahoo-plan-to-calendar/pkgs/container/wahoo-plan-to-calendar
    - Package should appear with `latest` tag listed

    **Step 3 — Set package visibility to Public (MANUAL — required once):**
    - Visit: https://github.com/j-kinyanjui?tab=packages
    - Click on `wahoo-plan-to-calendar` package
    - Click "Package settings" (gear icon)
    - Under "Danger Zone", change visibility to **Public**
    - Confirm the change

    **Step 4 — Test public pull (optional but recommended):**
    ```bash
    docker pull ghcr.io/j-kinyanjui/wahoo-plan-to-calendar:latest
    ```

    **If CD workflow failed:** Check the Actions log for error details and describe the failure.
  </how-to-verify>
  <resume-signal>Type "approved" if GHCR shows the `latest` image, or describe any issues found</resume-signal>
</task>

<task type="auto">
  <name>task 2: push v1.1.0 git tag to trigger versioned release</name>
  <files></files>
  <action>
Create and push the `v1.1.0` tag. This will:
1. Trigger the CI workflow (ci.yml runs on `v*` tags — already configured)
2. After CI succeeds, trigger the CD workflow via workflow_run
3. CD will build and push the Docker image with `v1.1.0` tag
4. CD will create a GitHub Release with auto-generated changelog and distZip artifact

Run:
```bash
git tag -a v1.1.0 -m "Release v1.1.0 — CI/CD Pipeline"
git push origin v1.1.0
```

Then monitor:
- Go to: https://github.com/j-kinyanjui/wahoo-plan-to-calendar/actions
- Wait for CI workflow on the v1.1.0 tag to complete (green)
- Wait for CD workflow to complete (green) — both `docker` and `release` jobs
- Check releases page: https://github.com/j-kinyanjui/wahoo-plan-to-calendar/releases

This satisfies **CD-02** (versioned image) and **CD-03** (GitHub Release).
  </action>
  <verify>
    <automated>
      # Confirm tag exists locally and remotely
      git tag --list "v1.1.0"
      git ls-remote --tags origin "refs/tags/v1.1.0"
    </automated>
  </verify>
  <done>
    - v1.1.0 tag created and pushed
    - CI workflow triggered on the tag
    - CD workflow triggered after CI success
    - Docker image with `v1.1.0` tag pushed to GHCR
    - GitHub Release created at https://github.com/j-kinyanjui/wahoo-plan-to-calendar/releases
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>checkpoint 2: verify versioned GHCR image and GitHub Release for v1.1.0</name>
  <action>Human verifies the CD workflow succeeded for the v1.1.0 tag — versioned Docker image in GHCR and GitHub Release with changelog and distZip asset.</action>
  <what-built>
    v1.1.0 tag pushed. CI ran on the tag. CD ran — published a versioned Docker image and created a GitHub Release with auto-generated changelog and distZip attached.
  </what-built>
  <how-to-verify>
    **Step 1 — Check workflow runs:**
    Visit: https://github.com/j-kinyanjui/wahoo-plan-to-calendar/actions
    - Confirm CI workflow ran on `v1.1.0` tag ✓
    - Confirm CD workflow ran and both jobs completed ✓
      - `Build & Push Docker Image` job ✓
      - `Create GitHub Release` job ✓

    **Step 2 — Verify versioned Docker image:**
    Visit: https://github.com/j-kinyanjui/wahoo-plan-to-calendar/pkgs/container/wahoo-plan-to-calendar
    - `v1.1.0` tag should appear alongside `latest`
    - Multi-platform support (linux/amd64, linux/arm64) visible in package details

    **Step 3 — Verify GitHub Release:**
    Visit: https://github.com/j-kinyanjui/wahoo-plan-to-calendar/releases
    - Release `v1.1.0` should appear
    - Auto-generated changelog should list commits/PRs since last tag
    - Docker pull command should be in the release body
    - distZip file should appear as a release asset

    **Step 4 — Verify public Docker pull (optional):**
    ```bash
    docker pull ghcr.io/j-kinyanjui/wahoo-plan-to-calendar:v1.1.0
    ```

    **CD-01 ✓** — `latest` tag from main merge (verified in previous checkpoint)
    **CD-02 ✓** — `v1.1.0` versioned image in GHCR
    **CD-03 ✓** — GitHub Release with changelog visible at releases page
  </how-to-verify>
  <resume-signal>Type "approved" when all three requirements verified, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
All three CD requirements verified by human in production GitHub environment:
- CD-01: `latest` image in GHCR after main merge
- CD-02: `v1.1.0` image in GHCR after tag push
- CD-03: GitHub Release at releases page with changelog and distZip asset
- GHCR package visibility set to Public
</verification>

<success_criteria>
- CI workflow completes green on both main push and v1.1.0 tag push
- CD workflow completes green on both triggers
- GHCR shows `latest` tag (from main merge) and `v1.1.0` tag (from tag push)
- GitHub Release for v1.1.0 exists with auto-generated changelog
- GitHub Release body contains Docker pull command
- distZip file attached to GitHub Release
- GHCR package visibility is Public
- Phase 6 success criteria (all 4) confirmed by human
</success_criteria>

<output>
After completion, create `.planning/phases/06-docker-release-pipeline/06-02-SUMMARY.md`
</output>
