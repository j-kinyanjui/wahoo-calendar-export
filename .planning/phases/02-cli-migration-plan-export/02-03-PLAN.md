---
phase: 02-cli-migration-plan-export
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - src/main/kotlin/nesski/de/cli/WahooCli.kt
  - src/main/kotlin/nesski/de/config/AppConfig.kt
autonomous: true
gap_closure: true
requirements: [CLI-01, CLI-02, PARSE-01, PARSE-02, PARSE-03, PARSE-04, DISP-01, DISP-02, DISP-03]

must_haves:
  truths:
    - "CLI-01 and CLI-02 have definitions in REQUIREMENTS.md with descriptions"
    - "CLI-01 and CLI-02 appear in the traceability table mapped to Phase 2"
    - "First-run credential prompt proceeds to auth→fetch→display without requiring re-run"
    - "First-run credential prompt saves credentials to TOML config file when user confirms"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CLI-01 and CLI-02 requirement definitions and traceability entries"
      contains: "CLI-01"
    - path: "src/main/kotlin/nesski/de/cli/WahooCli.kt"
      provides: "Credential prompt that proceeds with auth flow and saves to config"
      contains: "promptForCredentials"
    - path: "src/main/kotlin/nesski/de/config/AppConfig.kt"
      provides: "Config file writing capability for credential saving"
      contains: "save"
  key_links:
    - from: "src/main/kotlin/nesski/de/cli/WahooCli.kt"
      to: "src/main/kotlin/nesski/de/config/AppConfig.kt"
      via: "promptForCredentials calls AppConfig.save() to persist credentials"
      pattern: "AppConfig\\.save"
---

<objective>
Close two verification gaps from 02-VERIFICATION.md:

1. Add missing CLI-01 and CLI-02 requirement definitions to REQUIREMENTS.md with traceability entries
2. Fix promptForCredentials() in WahooCli.kt to use collected credentials for the auth→fetch→display flow (instead of returning early) and implement config file saving

Purpose: Complete requirements traceability chain and eliminate the first-run UX dead-end where prompted credentials are discarded.
Output: Updated REQUIREMENTS.md, WahooCli.kt with working credential prompt flow, AppConfig.kt with save capability
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cli-migration-plan-export/02-VERIFICATION.md
@.planning/phases/02-cli-migration-plan-export/02-01-SUMMARY.md
@.planning/phases/02-cli-migration-plan-export/02-02-SUMMARY.md
@src/main/kotlin/nesski/de/cli/WahooCli.kt
@src/main/kotlin/nesski/de/config/AppConfig.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CLI-01 and CLI-02 requirement definitions to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Add two new requirement definitions under a new "### CLI" section in REQUIREMENTS.md, placed after the "### Display" section:

```
### CLI

- [x] **CLI-01**: Application runs as a Clikt CLI with `--range`, `--from`/`--to`, `--config` options (no web server)
- [x] **CLI-02**: CLI loads credentials and settings from TOML config file (`~/.config/wahoo-cli/config`) with env var overrides (`SYSTM_USER`, `SYSTM_PASSWORD`)
```

Both are marked `[x]` because the functionality is already implemented (verified in 02-VERIFICATION.md).

Add CLI-01 and CLI-02 rows to the Traceability table at the bottom of the file:

```
| CLI-01      | Phase 2 | Complete |
| CLI-02      | Phase 2 | Complete |
```

Update the v1 requirements count from 14 to 16 in:
- The Coverage section: "v1 requirements: 16 total", "Mapped to phases: 16"
- The header area if it mentions a count

Do NOT change any existing requirement definitions or their status.
  </action>
  <verify>
    <automated>grep -c "CLI-01\|CLI-02" .planning/REQUIREMENTS.md | grep -q "4" && echo "PASS: CLI-01 and CLI-02 found in definitions and traceability" || echo "FAIL"</automated>
    <manual>Verify CLI-01 and CLI-02 definitions match ROADMAP phase 2 requirements list</manual>
  </verify>
  <done>CLI-01 and CLI-02 have definitions in the CLI section, appear in the traceability table mapped to Phase 2 with Complete status, and v1 requirement counts are updated to 16</done>
</task>

<task type="auto">
  <name>Task 2: Fix credential prompt to proceed with auth flow and save to config</name>
  <files>src/main/kotlin/nesski/de/cli/WahooCli.kt, src/main/kotlin/nesski/de/config/AppConfig.kt</files>
  <action>
**In AppConfig.kt:** Add a `save()` companion function that writes credentials to a TOML config file:

```kotlin
fun save(configPath: String, credentials: CredentialsConfig) {
    val file = File(configPath)
    file.parentFile?.mkdirs()
    val toml = buildString {
        appendLine("[credentials]")
        appendLine("username = \"${credentials.username}\"")
        appendLine("password = \"${credentials.password}\"")
    }
    file.writeText(toml)
    logger.info("Credentials saved to $configPath")
}
```

Place this inside the `companion object` block after `resolveCredentials()`. Use manual TOML string building (not ktoml encoder) since ktoml-file 0.7.0 doesn't have an encoder — it's read-only.

**In WahooCli.kt:** Refactor `promptForCredentials()` to return the credentials and let the caller proceed instead of returning early:

1. Change `promptForCredentials()` signature to return `Pair<String, String>` (username, password).

2. Move the config-saving logic into `promptForCredentials()` — when user confirms save, call `AppConfig.save(resolvedConfigPath, CredentialsConfig(user, pass))` and echo the chmod 600 warning. `promptForCredentials` needs the resolved config path, so pass it as a parameter: `promptForCredentials(resolvedConfigPath: String): Pair<String, String>`.

3. Remove the "Re-run the command" echo and the early return. Instead, return `Pair(user, pass)`.

4. In `run()`, where the blank credential check is, replace the early-return block:

**Before:**
```kotlin
if (username.isBlank() || password.isBlank()) {
    promptForCredentials()
    return
}
```

**After:**
```kotlin
val (finalUsername, finalPassword) = if (username.isBlank() || password.isBlank()) {
    promptForCredentials(resolvedConfigPath)
} else {
    Pair(username, password)
}
```

Then use `finalUsername` and `finalPassword` throughout the rest of `run()` (in the SystmAuthService constructor and the "Authenticated as" echo).

5. Remove the TODO comment at the old line 122.

The net effect: when no credentials are found, the user is prompted, credentials are optionally saved, and the auth→fetch→display flow proceeds in the same invocation.
  </action>
  <verify>
    <automated>cd /Users/kaethe/Documents/repos/joseph/wahoo-plan-to-calendar && ./gradlew compileKotlin 2>&1 | tail -5</automated>
    <manual>Run with no env vars and no config file — interactive prompt should appear and proceed to auth after entry</manual>
  </verify>
  <done>promptForCredentials() returns credentials and optionally saves to config file; run() uses returned credentials to proceed with auth→fetch→display without requiring a re-run; no TODO comments remain; compileKotlin succeeds</done>
</task>

</tasks>

<verification>
1. `grep -c "CLI-01" .planning/REQUIREMENTS.md` returns at least 2 (definition + traceability)
2. `grep -c "CLI-02" .planning/REQUIREMENTS.md` returns at least 2 (definition + traceability)
3. `grep "Re-run" src/main/kotlin/nesski/de/cli/WahooCli.kt` returns nothing (early return removed)
4. `grep "TODO" src/main/kotlin/nesski/de/cli/WahooCli.kt` returns nothing (TODO resolved)
5. `grep "AppConfig.save" src/main/kotlin/nesski/de/cli/WahooCli.kt` returns a match (save wired in)
6. `./gradlew compileKotlin` succeeds
</verification>

<success_criteria>
- CLI-01 and CLI-02 defined in REQUIREMENTS.md with traceability to Phase 2
- Credential prompt proceeds to auth→fetch→display flow without re-run
- Config file saving implemented when user confirms
- No TODO comments remain in WahooCli.kt
- Project compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-migration-plan-export/02-03-SUMMARY.md`
</output>
