---
phase: 02-cli-migration-plan-export
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/nesski/de/cli/WahooCli.kt
autonomous: true
gap_closure: true
requirements: [CLI-02]

must_haves:
  truths:
    - "Default --config option is ~/.config/wahoo-cli/config instead of src/main/resources/config.toml"
    - "AppConfig.load() expands home path correctly to user's home directory"
  artifacts:
    - path: "src/main/kotlin/nesski/de/cli/WahooCli.kt"
      provides: "Default --config value set to ~/.config/wahoo-cli/config"
      contains: "--config"
  key_links:
    - from: "src/main/kotlin/nesski/de/cli/WahooCli.kt"
      to: "src/main/kotlin/nesski/de/config/AppConfig.kt"
      via: "AppConfig.load() expands ~ to user home"
      pattern: "expandUser|resolveHome"
---

<objective>
Close CLI-02 gap from 02-VERIFICATION.md:

Change default --config path from src/main/resources/config.toml to ~/.config/wahoo-cli/config to match CLI-02 requirement specification.

Purpose: Complete CLI-02 requirement contract and fix credentials fallback path
Output: WahooCli.kt with correct default config path
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cli-migration-plan-export/02-VERIFICATION.md
@src/main/kotlin/nesski/de/cli/WahooCli.kt
@src/main/kotlin/nesski/de/config/AppConfig.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change default --config to ~/.config/wahoo-cli/config</name>
  <files>src/main/kotlin/nesski/de/cli/WahooCli.kt</files>
  <action>
In `WahooCli.kt`, locate the `--config` option definition (currently line ~31):

**Before:**
```kotlin
option(--config", default = "src/main/resources/config.toml")
```

**After:**
```kotlin
option("--config", default = "~/.config/wahoo-cli/config")
```

Ensure `AppConfig.load()` in the referenced file has logic to expand `~` to the user's home directory (e.g., using `File(configPath).expandUser()` or `Paths.get(configPath).expandUser()`).

If `expandUser()` does not exist, use manual expansion: `configPath.replace("~", System.getProperty("user.home"))`.

Verify that the path expansion happens in `AppConfig.load()` before opening the file.
  </action>
  <verify>
    <automated>grep '~/.config/wahoo-cli' src/main/kotlin/nesski/de/cli/WahooCli.kt | head -1</automated>
    <manual>Run with no --config flag — should default to ~/.config/wahoo-cli/config and fail gracefully if file doesn't exist</manual>
  </verify>
  <done>Default --config path is ~/.config/wahoo-cli/config; path expansion logic is in place; compilation succeeds</done>
</task>

</tasks>

<verification>
1. `grep '~/.config/wahoo-cli' src/main/kotlin/nesski/de/cli/WahooCli.kt` returns the new default
2. `./gradlew compileKotlin` succeeds
3. Running `./gradlew run --args=""` with no config file shows appropriate error message (not a crash)
</verification>

<success_criteria>
- Default --config changed from src/main/resources/config.toml to ~/.config/wahoo-cli/config
- Path expansion logic (~ → home directory) is present in AppConfig.load()
- Compilation succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-migration-plan-export/02-04-SUMMARY.md`
</output>
